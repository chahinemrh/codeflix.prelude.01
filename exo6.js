decapitalize = require('.exo5');
module.exports = function kebabCase (str=''){
    let result ='' ; 
    //result += str.replace(/haha/i, 'hihi');

    //result += str.replace(/([a-z])([A-Z])/g, "$1-$2")
                // .replace(/\s+/g, '-')
                 //.toLowerCase();
    let i = 0 ; 
    while (i != str.length){
        decapitalize('str');

        if (str[i]=== ' ') {
            str.replace ()
        } 
    }
}
